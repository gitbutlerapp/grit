Upstream test `tests/t7301-clean-interactive.sh` — "git clean -i basic tests"

Fresh run 2026-06-04: 8/23 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 7: git clean -id (filter patterns)
- 8: git clean -id (filter patterns 2)
- 9: git clean -id (select - all)
- 11: git clean -id (select - number)
- 12: git clean -id (select - number 2)
- 13: git clean -id (select - number 3)
- 14: git clean -id (select - filenames)
- 15: git clean -id (select - range)
- 16: git clean -id (select - range 2)
- 17: git clean -id (inverse select)
- 18: git clean -id (ask)
- 19: git clean -id (ask - Ctrl+D)
- 20: git clean -id with prefix and path (filter)
- 21: git clean -id with prefix and path (select by name)
- 22: git clean -id with prefix and path (ask)

Repro: `./scripts/run-tests.sh t7301-clean-interactive.sh`
Full TAP log from this run: `logs/ticket-runs/t7301-clean-interactive.log`