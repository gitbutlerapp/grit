Upstream test `tests/t1430-bad-ref-name.sh` — "Test handling of ref names that check-ref-format rejects"

Fresh run 2026-06-04: 14/42 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 4: git branch shows badly named ref as warning
- 8: branch -D cannot delete ref in .git dir
- 11: branch -m cannot rename to a bad ref name
- 15: rev-parse skips symref pointing to broken name
- 16: for-each-ref emits warnings for broken names
- 18: branch -d can delete broken name
- 21: update-ref --no-deref -d can delete dangling symref to broken name
- 22: branch -d can delete dangling symref to broken name
- 23: update-ref -d can delete broken name through symref
- 24: update-ref --no-deref -d can delete symref with broken name
- 25: branch -d can delete symref with broken name
- 26: update-ref --no-deref -d can delete dangling symref with broken name
- 27: branch -d can delete dangling symref with broken name
- 28: update-ref -d cannot delete non-ref in .git dir
- 30: update-ref --stdin fails create with bad ref name
- 31: update-ref --stdin fails update with bad ref name
- 32: update-ref --stdin fails delete with bad ref name
- 33: update-ref --stdin -z fails create with bad ref name
- 34: update-ref --stdin -z fails update with bad ref name
- 35: update-ref --stdin -z fails delete with bad ref name
- 37: checkout -b rejects HEAD as a branch name
- 38: update-ref can operate on refs/heads/HEAD
- 39: branch -d can remove refs/heads/HEAD
- 40: branch -m can rename refs/heads/HEAD
- 41: branch -d can remove refs/heads/-dash
- 42: branch -m can rename refs/heads/-dash

Repro: `./scripts/run-tests.sh t1430-bad-ref-name.sh`
Full TAP log from this run: `logs/ticket-runs/t1430-bad-ref-name.log`