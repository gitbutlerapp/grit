Upstream test `tests/t5705-session-id-in-capabilities.sh` — "session ID in capabilities"

Fresh run 2026-06-04: 9/17 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 3: session IDs not advertised by default (push v0)
- 5: session IDs not advertised by default (push v1)
- 7: session IDs not advertised by default (push v2)
- 10: session IDs advertised (push v0)
- 13: session IDs advertised (push v1)
- 15: session IDs advertised (fetch v2)
- 16: session IDs advertised (push v2)
- 17: client & server log negotiated version (v2)

Repro: `./scripts/run-tests.sh t5705-session-id-in-capabilities.sh`
Full TAP log from this run: `logs/ticket-runs/t5705-session-id-in-capabilities.log`