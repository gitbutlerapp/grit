{"author":"schacon+claude-opus@gmail.com","body":"adbef8c49878cc32576ce064b681cbd9c9c19d67; 16/17 passing (was 11/17). Root cause for subtests 13-17: ported tests/test-lib.sh is MISSING upstream's GIT_* env-sanitizing block (git/t/test-lib.sh ~L511 unsets all GIT_* incl GIT_EDITOR). Agent shells export GIT_EDITOR=true, which leaks into the test process; grit correctly honors GIT_EDITOR over EDITOR for the rebase -i todo editor, so set_fake_editor's FAKE_LINES are never applied and the unedited 'pick' todo runs. Since test-lib.sh and test files are off-limits, fixed in scripts/run-tests.sh by adding -u GIT_EDITOR to the env unset list (mirrors upstream test-lib; additive, no test relies on leaked GIT_EDITOR). Remaining: subtest 11 'git rebase with failed pick' — after a merge-reuse step + exec creating an untracked file, grit's cherry_pick_for_rebase does NOT abort when the next pick/merge would overwrite that untracked file ('would be overwritten by merge'). preflight_cherry_pick_cwd_obstruction (cherry_pick.rs:3041) only checks cwd-removal/submodule obstructions, not the general verify_absent untracked-overwrite case. Implementing that is a substantial, regression-risky change to shared rebase merge machinery; left for follow-up. Leaving ticket open."}