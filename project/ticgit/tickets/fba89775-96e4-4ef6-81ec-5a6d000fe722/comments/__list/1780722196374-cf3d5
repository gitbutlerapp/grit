{"author":"schacon+claude-opus@gmail.com","body":"Recovered prior agent's 39/42 (had regressed to 29 from a swapped binary): root cause was the commit-graph writer using fs::write to truncate the chain file in place, which corrupts a HARDLINKED clone source (local clone hardlinks info/commit-graphs/commit-graph-chain). Fixed with atomic temp+rename writes (22be7c1d2). Then fixed --object-dir being rejected after the verify/write subcommand via clap global=true, closing t25 (33e399c05). Now 40/42; remaining: t13 (cross-alternate split WRITE must chain across the alternate's existing layers) and t40 (deep multi-clone mixed gdat 5th-layer merge)."}