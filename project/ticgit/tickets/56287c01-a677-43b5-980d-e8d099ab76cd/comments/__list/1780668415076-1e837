{"author":"schacon+claude-opus@gmail.com","body":"89/98 passing (was 83). Fixed this session: 8 (validate_midx_referenced_packs emits 'packfile <pack> index unavailable' once at pack-store prep, even when objects are loose), 25 (pack-objects writes+prints empty pack to file unless --non-empty), 56 (repack preserves MIDX unless a referenced pack is removed, mirroring repack_remove_redundant_pack), 61 (repack --local incremental path excludes alternate objects), 76+97 (incremental repack excludes already-packed objects gated on --incremental not --no-write-bitmap-index, so BTMP bitmap_nr=3/pack). Commits: 58c752a, 717e402, 66fb034, 4582674, aa76ab8 (the --incremental pack_objects change landed in HEAD via f6fe0c217 due to GitButler workspace coupling). Remaining 9 all deep/outside-MIDX: 23 (full sha256 object reading - repack -a dies SHA-1 mismatch in sha256 repo), 63/64/65/94 (byte-exact 64-bit idx layout: corrupt at idxoff=2999 assumes git's exact v2 idx; grit MIDX write rejects the forced large offset; 94 reuses objects64), 77/78/81 (byte-exact expire batch: MIDX object->pack attribution differs so grit expires pack-B that git keeps; 81 depends on 77/78 state), 98 (needs full EWAH commit bitmaps + test-tool 'bitmap' subcommand + preferBitmapTips hierarchy; grit writes placeholder empty .bitmap). See logs/2026-06-05T13:03-t5319-multi-pack-index.md."}