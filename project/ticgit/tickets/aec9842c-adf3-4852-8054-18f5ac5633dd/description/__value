Upstream test `tests/t3600-rm.sh` — "Test of the various options to git rm."

Fresh run 2026-06-04: 73/82 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 59: rm of a conflicted populated submodule with a .git directory fails even when forced
- 61: rm of a populated submodule with a .git directory migrates git dir
- 62: setup subsubmodule
- 63: rm recursively removes work tree of unmodified submodules
- 64: rm of a populated nested submodule with different nested HEAD fails unless forced
- 65: rm of a populated nested submodule with nested modifications fails unless forced
- 66: rm of a populated nested submodule with nested untracked files fails unless forced
- 67: rm absorbs submodule's nested .git directory
- 68: checking out a commit after submodule removal needs manual updates

Context from retired plan files:
- (plan.md) 6. `rm`, `add`, and interactive patch: `t3701`, `t3600`, `t3700`, `t3702`.

Repro: `./scripts/run-tests.sh t3600-rm.sh`
Full TAP log from this run: `logs/ticket-runs/t3600-rm.log`