Upstream test `tests/t7508-status.sh` — "git status"

Fresh run 2026-06-04: 121/126 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 13: commit ignores status.displayCommentPrefix=false in COMMIT_EDITMSG
- 65: commit with submodule summary ignores status.displayCommentPrefix
- 119: --no-optional-locks prevents index update
- 120: racy timestamps will be fixed for clean worktree
- 121: racy timestamps will be fixed for dirty worktree

Context from retired plan files:
- (plans/status-perf-plan.md) - Existing harness: **`t7508`**, **`t7063`** (untracked cache), **`t0008`** / ignore tests after refactors.

Repro: `./scripts/run-tests.sh t7508-status.sh`
Full TAP log from this run: `logs/ticket-runs/t7508-status.log`