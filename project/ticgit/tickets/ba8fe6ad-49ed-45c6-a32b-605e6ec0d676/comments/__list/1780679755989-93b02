{"author":"schacon+claude-opus@gmail.com","body":"18d54d605; 22/22 passing; added pull flags -v/--verbose (Git signed-verbosity counter via argv scan), --all (delegates to fetch --all then merges from FETCH_HEAD), --force, --dry-run (early-return, write nothing), --allow-unrelated-histories, --signoff/--no-signoff, --no-verify/--verify (overrides_with so last wins); wired into fetch/merge args. Also fixed 'show -s --pretty=format:%(trailers)' (show.rs apply_format_string had no %( branch) by reusing log::parse_trailers_opts + commit_trailers::format_trailers."}