Upstream test `tests/t3700-add.sh` — "Test of git add, including the -- option."

Fresh run 2026-06-04: 56/58 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 24: git add --refresh
- 25: git add --refresh with pathspec

Context from retired plan files:
- (plan.md) 6. `rm`, `add`, and interactive patch: `t3701`, `t3600`, `t3700`, `t3702`.

Repro: `./scripts/run-tests.sh t3700-add.sh`
Full TAP log from this run: `logs/ticket-runs/t3700-add.log`