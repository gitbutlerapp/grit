Upstream test `tests/t4015-format-patch-options.sh` — "format-patch extended options (base, signoff, threading, MIME, keep-subject)"

Fresh run 2026-06-04: 2/12 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 2: format-patch --base=<commit> adds base-commit info
- 4: format-patch -s adds Signed-off-by
- 5: format-patch --signoff adds Signed-off-by
- 6: format-patch --in-reply-to adds In-Reply-To and References
- 7: format-patch --cc adds Cc header
- 8: format-patch --to adds To header
- 9: format-patch --cc and --to can be combined
- 10: format-patch --attach produces MIME attachment
- 11: format-patch --inline produces MIME inline
- 12: format-patch -k keeps subject without [PATCH] prefix

Repro: `./scripts/run-tests.sh t4015-format-patch-options.sh`
Full TAP log from this run: `logs/ticket-runs/t4015-format-patch-options.log`