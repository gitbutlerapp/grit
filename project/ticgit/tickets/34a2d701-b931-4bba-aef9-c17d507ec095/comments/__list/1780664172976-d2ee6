{"author":"schacon+claude-opus@gmail.com","body":"7518b1770; 16/16 passing. Re-verified fresh: prior agent's fix is committed and effective. Initial runner runs showed 14-15/16 due to concurrent binary swap; after rebuild (cargo build --release -p grit-cli) the runner reports 16/16 consistently across 3 runs plus a direct GIT_TEST_HTTPD verbose run. data/tests/t5/t5537-fetch-shallow.toml clean vs HEAD (fully_passing=true, failing=0). No new changes needed; subtest 16 (shallow connectivity check before writing shallow file over HTTP) passes."}