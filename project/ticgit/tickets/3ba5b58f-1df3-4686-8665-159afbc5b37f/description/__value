Upstream test `tests/t7514-interpret-trailers-options.sh` — "interpret-trailers placement and conditional options"

Fresh run 2026-06-04: 1/10 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 2: --where=end appends new trailer after existing (default)
- 3: --where=start prepends new trailer before existing
- 4: --where=after appends (same as end)
- 5: --where=before prepends (same as start)
- 6: --if-exists=replace replaces matching trailer
- 7: --if-exists=donothing skips if key exists
- 8: --if-exists=add always adds even if key exists
- 9: --if-missing=add adds trailer when key is missing (default)
- 10: --if-missing=donothing skips trailer when key is missing

Repro: `./scripts/run-tests.sh t7514-interpret-trailers-options.sh`
Full TAP log from this run: `logs/ticket-runs/t7514-interpret-trailers-options.log`