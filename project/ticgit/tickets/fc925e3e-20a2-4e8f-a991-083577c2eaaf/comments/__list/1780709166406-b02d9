{"author":"schacon+claude-opus@gmail.com","body":"4eb912cf7; 111/115 passing (was 54/115). Fixed: SSH transport (added shell git/t/helper/test-fake-ssh; faithful host_end/get_host_and_port/get_port + path[1]=='~' parsing for [user@::1], ~repo, [myhost:123]; -G probe ordering '-G <opts> <host>'; simple-variant option validation propagated on local-resolve; GIT_SSH_COMMAND argv recording + plink cmdline basename detection; truncate ssh-output), absolute separate-git-dir gitfile, --branch=<tag> default heads refspec when not --single-branch, and remote.<name>.promisor=false opting out of lazy-fetch. Remaining 4 are deep features: 108 needs trace2 total_rounds + single-batch checkout fetch (grit emits no total_rounds; checkout lazy-fetches per-blob); 113/114/115 need protocol-v2 bundle-uri auto-discover over HTTP spawning git-remote-https child + trace2 child_start. Details in logs/2026-06-06T00:39-t5601-clone.md."}