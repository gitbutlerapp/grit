Upstream test `tests/t1461-refs-list.sh` — "git refs list tests"

Fresh run 2026-06-04: 417/428 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 204: Check format "default-local" date fields output
- 208: Check format "short-local" date fields output
- 209: Check format "local" date fields output
- 211: Check format "iso8601-local" date fields output
- 213: Check format "rfc2822-local" date fields output
- 215: Check format "raw-local" date fields output
- 217: Check format of strftime-local date fields
- 384: if arguments, %(contents:trailers) shows error if colon is missing
- 387: HEAD atom does not take arguments
- 388: subject atom rejects unknown arguments
- 389: refname atom rejects unknown arguments

Context from retired plan files:
- (plan.md) - Current refs focus: `t1461-refs-list.sh` is now 359/428 after tracking atom fixes.

Repro: `./scripts/run-tests.sh t1461-refs-list.sh`
Full TAP log from this run: `logs/ticket-runs/t1461-refs-list.log`