Upstream test `tests/t3920-crlf-messages.sh` — "Test ref-filter and pretty APIs for commit and tag messages using CRLF"

Fresh run 2026-06-04: 3/18 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 2: branch: --verbose works with messages using CRLF
- 3: branch: --format='%(contents:subject)' works with messages using CRLF
- 4: branch: --format='%(contents:body)' works with messages using CRLF
- 5: branch: --format='%(contents)' works with messages using CRLF
- 6: tag: --format='%(contents:subject)' works with messages using CRLF
- 7: tag: --format='%(contents:body)' works with messages using CRLF
- 8: tag: --format='%(contents)' works with messages using CRLF
- 9: for-each-ref: --format='%(contents:subject)' works with messages using CRLF
- 10: for-each-ref: --format='%(contents:body)' works with messages using CRLF
- 12: log: --oneline works with messages using CRLF
- 13: log: --format='%s' works with messages using CRLF
- 14: log: --format='%b' works with messages using CRLF
- 16: show: --format='%s' works with messages using CRLF
- 17: show: --format='%b' works with messages using CRLF
- 18: show: --format='%B' works with messages using CRLF

Context from retired plan files:
- (plan.md) 7. Stash/i18n/precompose/CRLF messages: `t3903`, `t3900`, `t3920`, `t3905`, `t3906`,

Repro: `./scripts/run-tests.sh t3920-crlf-messages.sh`
Full TAP log from this run: `logs/ticket-runs/t3920-crlf-messages.log`