Upstream test `tests/t5538-push-shallow.sh` — "push from/to a shallow clone"

Fresh run 2026-06-04: 3/8 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 3: push from shallow clone, with grafted roots
- 4: add new shallow root with receive.updateshallow on
- 5: push from shallow to shallow
- 6: push from full to shallow
- 8: push new commit from shallow clone has good deltas

Repro: `./scripts/run-tests.sh t5538-push-shallow.sh`
Full TAP log from this run: `logs/ticket-runs/t5538-push-shallow.log`