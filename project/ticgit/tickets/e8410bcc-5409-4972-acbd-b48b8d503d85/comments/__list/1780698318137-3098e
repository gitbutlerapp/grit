{"author":"schacon+claude-opus@gmail.com","body":"240c9e043 (grit-t5-progress); 13/13 passing. Fixes: (1) opportunistic tag-follow is now add-only — a default fetch no longer rejects a moved auto-followed tag with 'would clobber' (test 3); (2) 'clone --branch <tag>' supported — detached HEAD at tag, '+refs/tags/<tag>:refs/tags/<tag>' refspec, follow object-present tags, and fetch the tag object even under --no-tags (tests 9/10); (3) '--single-branch' from a detached source HEAD writes a url-only remote with no fetch refspec and no remote-tracking refs (test 13). Source changes in grit/src/commands/{clone,fetch}.rs (GitButler absorbed the hunks into the branch). NOTE: t5515 (1/65) and t5611 (11/13) are failing but VERIFIED pre-existing — reproduced identically by building with HEAD's fetch.rs (my changes reverted); broken by another agent's in-flight clone/transport work (peeled ^{} tag-follow on plain clone; 'clone -c remote.origin.fetch' dropping default refspec)."}