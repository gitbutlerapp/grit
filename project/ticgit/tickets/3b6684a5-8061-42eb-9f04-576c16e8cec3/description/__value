Upstream test `tests/t5600-clone-fail-cleanup.sh` — "test git clone to cleanup after failure This test covers the fact that if git clone fails, it should remove the directory it created, to avoid the user having to manually remove the directory before attempting a clone again. Unless the directory already exists, in which case we clean up only what we wrote."

Fresh run 2026-06-04: 8/14 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 8: failed clone --separate-git-dir should not leave any directories
- 9: failed clone into empty leaves directory (vanilla)
- 10: failed clone into empty leaves directory (bare)
- 11: failed clone into empty leaves directory (separate)
- 12: failed clone into empty leaves directory (separate, git)
- 13: failed clone into empty leaves directory (separate, wt)

Repro: `./scripts/run-tests.sh t5600-clone-fail-cleanup.sh`
Full TAP log from this run: `logs/ticket-runs/t5600-clone-fail-cleanup.log`