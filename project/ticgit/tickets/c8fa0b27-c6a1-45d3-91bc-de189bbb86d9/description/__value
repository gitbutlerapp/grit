Upstream test `tests/t6415-merge-dir-to-symlink.sh` — "merging when a directory was replaced with a symlink"

Fresh run 2026-06-04: 13/24 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 2: checkout does not clobber untracked symlink
- 8: Handle D/F conflict, do not lose a/b-2/c/d in merge (recursive)
- 9: a/b was resolved as symlink
- 12: Handle F/D conflict, do not lose a/b-2/c/d in merge (recursive)
- 13: a/b was resolved as symlink
- 14: do not lose untracked in merge (resolve)
- 16: do not lose modifications in merge (resolve)
- 21: merge should not have D/F conflicts (recursive)
- 22: a/b-2 was resolved as symlink
- 23: merge should not have F/D conflicts (recursive)
- 24: a/b-2 was resolved as symlink

Context from retired plan files:
- (plan.md) - Merge machinery: `t6400`, `t6402`, `t6404`, `t6406`, `t6411`, `t6414`, `t6415`,
- (t6-plan.md) - [ ] `t6415-merge-dir-to-symlink.sh` — 13/24 passing, 11 failing.

Repro: `./scripts/run-tests.sh t6415-merge-dir-to-symlink.sh`
Full TAP log from this run: `logs/ticket-runs/t6415-merge-dir-to-symlink.log`