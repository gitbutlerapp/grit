Upstream test `tests/t4058-diff-duplicates.sh` — "test tree diff when trees have duplicate entries"

Fresh run 2026-06-04: 10/16 pass. To close this ticket, implement/fix grit behavior so these subtests pass:

- 8: git read-tree does not segfault
- 9: reset --hard does not segfault
- 10: git diff HEAD does not segfault
- 11: can switch to another branch when status is empty
- 14: clean status, switch branches, status still clean
- 15: switch to base branch and force status to be clean

Repro: `./scripts/run-tests.sh t4058-diff-duplicates.sh`
Full TAP log from this run: `logs/ticket-runs/t4058-diff-duplicates.log`